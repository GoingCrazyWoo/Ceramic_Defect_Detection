# 测试和调试工具

本目录包含ROI检测的测试和可视化工具。

## 🛠️ 工具列表

### 1. `test_roi_detection.m`
**功能：** 测试ROI检测的不同参数组合

**使用方法：**
```matlab
cd tools
test_roi_detection
```

**输出：**
- 6个子图对比不同参数的ROI效果
- ROI覆盖率统计
- 推荐最优参数

### 2. `test_roi_threshold.m`
**功能：** 测试不同亮度阈值（0.10-0.35）的效果

**使用方法：**
```matlab
cd tools
test_roi_threshold
```

**输出：**
- 6组对比图（不同阈值）
- 覆盖率vs阈值趋势图
- 区域数vs阈值趋势图
- 自动推荐最优阈值

### 3. `visualize_roi_detail.m`
**功能：** 详细可视化当前ROI检测效果

**使用方法：**
```matlab
cd tools
visualize_roi_detail
```

**输出：**
- 8个子图详细展示ROI
  - 原始图
  - 亮度分布
  - ROI掩码
  - ROI边界
  - 彩色叠加
  - 只保留ROI
  - 水平切片分析
  - 垂直切片分析
- 详细统计信息
- 诊断和建议

## 📊 使用场景

### 场景1：ROI检测不准确
```matlab
cd tools
visualize_roi_detail  % 查看问题
test_roi_threshold    % 找最优阈值
```

### 场景2：新图像测试
```matlab
cd tools
test_roi_detection  % 快速测试多个参数
```

### 场景3：参数微调
```matlab
cd tools
% 修改test_roi_detection.m中的参数
% 重新运行查看效果
```

## 💡 技巧

1. **对比测试**
   - 先运行`test_roi_threshold`找到合适的阈值范围
   - 再运行`visualize_roi_detail`详细检查

2. **快速迭代**
   - 在`visualize_roi_detail.m`中直接修改参数
   - 重新运行立即看到效果

3. **保存最优参数**
   - 找到最优参数后，更新`../ceramic_defect_pipeline.m`
   - 或在`auto_run.m`中指定

## ⚙️ 常用参数调整

### 如果ROI覆盖率太高（>60%）
```matlab
% 提高亮度阈值
'BrightnessThreshold', 0.35
```

### 如果ROI覆盖率太低（<30%）
```matlab
% 降低亮度阈值
'BrightnessThreshold', 0.20
```

### 如果管子被连成一片
```matlab
% 减小闭运算半径
'MorphCloseRadius', 10
```

### 如果漏检了小管子
```matlab
% 降低最小面积要求
'MinAreaRatio', 0.02
```

## 📝 注意事项

- 这些工具仅用于测试和调试
- 不要在生产环境中使用
- 修改参数后记得更新主代码
- 运行前确保在`matlab/tools/`目录下

## 🔗 相关文档

- `../README.md` - 主要使用说明
- `../../versions/v3.0_roi_detection/docs/` - v3.0完整文档
- `../../PROJECT_CLEANUP_PLAN.md` - 项目结构说明

